version: '3.9'

services:
  caravan-trip-cost-calculator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: caravan-trip-cost-calculator
    restart: unless-stopped
    ports:
      - '8083:80'
